**THIS FOLDER MUST BE DELETED IN THE RELEASE**

# Description
These are files that they don't link with an `#include "example.h"` on the main source file. Instead, for be a common use, those files compiles into an *Object file* and next, Link with the main project. You're free to use this *Common Source files*. Remember that the majority of files will be writted on **C** for basic or intermediate process, and **C++** for advanced process.
